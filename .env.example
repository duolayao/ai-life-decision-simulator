# DeepSeek API 配置示例
# 复制本文件为 .env 并填入真实的 API Key
#
# ⚠ 重要说明：
# - 这些变量仅供本地开发（npm run dev）使用，VITE_ 前缀会被 Vite 打包进 dist 产物。
# - 生产部署时不要把 .env 里的 Key 同步到服务器构建，否则 Key 会泄露到前端产物。
#
# 生产环境部署方案（推荐 - 走 Nginx 反代）：
# 1. 在服务器 Nginx 配置里把 /api/deepseek/v1/* 转发到 https://api.deepseek.com/v1/*
# 2. 在 Nginx 的 proxy_set_header 里注入 Authorization: Bearer <你的真实 Key>
# 3. 前端代码会自动走相对路径 /api/deepseek/v1/chat/completions，无需 Key
# 4. 服务器上不需要配置 .env，npm run build 出来的产物可直接部署
# 详见部署文档：deploy/nginx.conf

# 本地开发用变量（生产环境忽略这些）
VITE_DEEPSEEK_API_KEY=your_api_key_here
VITE_DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
VITE_DEEPSEEK_MODEL=deepseek-chat
